# Extensions and contracts

Capability modules are trusted build-time code and are statically listed in `@fonat/module-registry`. Content packages contain data only and cannot install executable behavior. Registered exercise identifiers are generated from the shared contract union. The Project capability can be disabled without removing stored Project records.
