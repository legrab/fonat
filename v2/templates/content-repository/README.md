# Fonat content repository

Maintain custom educational content independently from the application code. Copy a complete valid package under `content/`, run validation locally, and import the resulting folder or ZIP into Fonat.

During Fonat alpha development, install the packed CLI tarball produced by the main repository. After publication, pin the exact npm version and commit `package-lock.json`.
